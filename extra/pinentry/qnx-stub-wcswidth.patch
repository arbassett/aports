--- pinentry-1.3.1-base/pinentry/pinentry-curses.c	2024-07-03 09:29:18.000000000 +0000
+++ pinentry-1.3.1/pinentry/pinentry-curses.c	2024-07-03 09:29:18.000000000 +0000
@@ -95,6 +95,20 @@
 
 #ifdef HAVE_NCURSESW
 typedef wchar_t CH;
+int
+wcswidth(const wchar_t *str, size_t n) {
+	int len = wcslen(str);
+	if(len > n) {
+		len = n;
+	}
+	return len;
+}
+
+int
+wcwidth(wchar_t ch) {
+	return 1;
+}
+
 #define STRLEN(x) wcslen (x)
 #define STRWIDTH(x) wcswidth (x, wcslen (x))
 #define ADDCH(x) addnwstr (&x, 1);
